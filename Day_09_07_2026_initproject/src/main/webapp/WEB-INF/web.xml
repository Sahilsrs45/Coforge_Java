<?xml version="1.0" encoding="UTF-8"?>

<web-app
    xmlns="https://jakarta.ee/xml/ns/jakartaee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                        https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
    version="6.0">

    <display-name>Demo Servlet Application</display-name>

    <!-- Welcome Page -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Application Context Parameters -->
    <context-param>
        <param-name>url</param-name>
        <param-value>jdbc:mysql://localhost:3306/empdb</param-value>
    </context-param>

    <context-param>
        <param-name>username</param-name>
        <param-value>root</param-value>
    </context-param>

    <context-param>
        <param-name>password</param-name>
        <param-value>db123</param-value>
    </context-param>

    <!-- Servlet Configuration -->
    <servlet>
        <servlet-name>demo</servlet-name>
        <servlet-class>com.coforge.DemoServlet</servlet-class>

        <!-- Servlet Init Parameter -->
        <init-param>
            <param-name>servletName</param-name>
            <param-value>Demo Servlet</param-value>
        </init-param>

        <load-on-startup>1</load-on-startup>
    </servlet>

    <!-- Servlet Mapping -->
    <servlet-mapping>
        <servlet-name>demo</servlet-name>
        <url-pattern>/DemoServlet</url-pattern>
    </servlet-mapping>

</web-app>